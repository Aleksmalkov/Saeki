import React from 'react';
import AppRouter from './router/router';  // Import the router here
import './App.css';

const App: React.FC = () => {
  return <AppRouter />;  // Use the AppRouter component to handle routing
};

export default App;
